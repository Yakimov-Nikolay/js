function textPrint(param){

    let text = param[0];

    for(let i =0; i <text.length; i ++) {
            let letter = text[i];
            console.log(letter);
    }

}
textPrint(["softuni"]);
